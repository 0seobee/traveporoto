# 여행 플래너 멀티유저 개방 PRD

- 작성일: 2026-07-20
- 상태: 초안 (검토 대기)
- 관련 코드: `planner.html`, `pin-gate.js`, `hub.html`, `proto.html`

## 1. 배경 및 목표

### 배경

현재 `planner.html`은 개인(가족) 용도로 만든 멀티 프로젝트 여행 플래너다. Supabase 백엔드(`ciljiutblgbnwlgcdiyq` 프로젝트)를 통해 여행별 일정·가계부·체크리스트·사진 갤러리를 관리하고 있으며, 페이지 단위 4자리 PIN(`pin-gate.js`)으로 접근을 제어한다.

다만 지금 구조는 "사람"이 아니라 "페이지"만 구분한다. `loadProjects()`가 Supabase `projects` 테이블 전체를 필터 없이 조회하기 때문에, 이 링크를 다른 사람에게 공유하면 내가 만든 모든 여행이 그대로 노출되고, 그 사람이 만든 여행도 나에게 다 보이게 된다. 지인들에게 자유롭게 열어주려면 "각자의 데이터가 서로 섞이지 않는" 구조가 먼저 필요하다.

### 목표

1. 지인 누구나 설치나 회원가입 없이 곧바로 자기만의 여행 계획표를 만들 수 있다.
2. 모든 기능은 100% 무료로 유지된다 (특정 인원 이상 쓰면 유료로 전환되는 기능이 없어야 함).
3. 각자의 데이터는 기본적으로 서로 침범되지 않는다. 다만 가족 여행처럼 여러 명이 같은 여행을 같이 편집해야 하는 경우도 지원한다.

### 성공 기준

- 지인이 링크를 열고 바로 자기 여행을 만들고, 일정/가계부/사진을 채울 수 있다 (가입 절차 0단계).
- 원할 때만 로그인해서 다른 기기와 동기화하거나, 다른 사람을 초대해 같이 편집할 수 있다.
- 내가(0seobee) 기존에 만들어둔 여행(포르투갈, 광저우 등)은 새 구조 도입 후에도 그대로 접근 가능하다.

## 2. 범위

### Goals

- 로컬 우선 저장 (기본값 — 로그인 없이 브라우저에만 저장)
- 선택적 구글 로그인 (Supabase Auth Google OAuth)
- 선택적 클라우드 동기화 (로그인한 사람만, 프로젝트 단위로 켜고 끌 수 있음)
- 공유 링크 기반 공동 편집 (초대받으면 소유자와 동일한 편집 권한)
- 기존 프로젝트 데이터를 사용자 본인 계정 소유로 이관

### Non-goals

- 네이티브 모바일 앱 (iOS/Android 스토어 배포) — 개발 기간·연 등록비($99/년 등)가 "완전 무료" 원칙과 충돌
- 다른 사람의 여행을 둘러보는 공개 탐색/발견 기능
- 열람 전용(뷰어) vs 편집자 같은 세분화된 권한 구분 — 초대받으면 전부 동일한 편집 권한
- 결제/구독 기능
- 실시간 동시 편집 시 충돌 해결(operational transform 등) — 마지막 저장이 이기는 방식으로 충분

## 3. 사용자 시나리오

| 시나리오 | 흐름 |
|---|---|
| A. 처음 써보는 지인 | 링크 접속 → 로그인 없이 "새 여행 만들기" → 브라우저(IndexedDB)에 바로 저장되어 사용 |
| B. 다른 기기에서도 보고 싶어짐 | 프로젝트 설정에서 "클라우드 동기화 켜기" → 구글 로그인 → 로컬 데이터가 Supabase로 업로드되고, 이후 다른 기기에서 같은 계정으로 로그인하면 이어서 볼 수 있음 |
| C. 가족 여행 공동 편집 | 클라우드 동기화 켠 여행에서 "공유 링크 만들기" → 카톡 등으로 전달 → 상대방이 링크 열고 구글 로그인하면 자동으로 편집자로 합류 |
| D. 기존 사용자 데이터 이관 | 로그인 도입 시점에, 현재 Supabase에 있는 기존 여행들(포르투갈, 광저우 등)의 소유자를 0seobee 구글 계정으로 일괄 지정 → 로그인하면 예전처럼 다 보임 |

## 4. 로드맵

### Phase 1 — 로컬 전용 (우선 출시)

- IndexedDB에 `projects / tabs / tab_items / gifts / ledger / checklist / photos`를 미러링하는 로컬 스키마 구축
- 사진은 Blob으로 IndexedDB에 저장 (용량이 크므로 저장 용량 경고 UX 필요)
- 로그인 개념 없음 — 브라우저(기기)당 하나의 독립된 저장소
- **데이터 내보내기/가져오기(JSON 백업) 기능 필수** — 브라우저 데이터 삭제나 기기 변경 시 데이터를 잃지 않는 유일한 안전망이므로 이 단계에서 반드시 함께 출시

### Phase 2 — 클라우드 동기화 + 공유 (후속 출시)

- Supabase Auth의 Google OAuth 도입
- `projects` 테이블에 `owner_id`(auth.users 참조) 컬럼 추가
- `project_members`(project_id, user_id, joined_at) 테이블 신설 — 공유 초대받은 사람 기록
- RLS를 현재의 "전체 허용"에서 "owner이거나 project_members에 속한 사람만 접근 가능"으로 전환 (`tabs`, `tab_items`, `gifts`, `ledger`, `checklist`, `photos` 등 하위 테이블도 project_id 경유로 동일 규칙 적용)
- "클라우드 동기화 켜기" 액션 시, 해당 프로젝트의 로컬 IndexedDB 데이터를 Supabase로 1회 업로드하는 마이그레이션 로직
- 공유 링크(`.../planner.html?join=<토큰>`) 발급 — 링크로 로그인하면 `project_members`에 자동 추가. 소유자가 링크를 껐다 재발급할 수 있어야 함
- 기존 Supabase에 남아있는 여행 데이터는 이 단계 배포 시 0seobee 계정으로 owner_id 일괄 이관
- `planner.html`에서는 `pin-gate.js` 제거(로그인+RLS가 그 역할을 대체). `proto.html`, `hub.html` 등 다른 개인용 페이지는 변경하지 않음

## 5. 아키텍처 요약

- **Phase 1**: 순수 클라이언트 사이드, 백엔드 없음 → 비용 0, 유출 리스크 0
- **Phase 2**: 기존 Supabase 프로젝트(`ciljiutblgbnwlgcdiyq`)를 그대로 재사용. 새 인프라를 따로 만들지 않음
- 두 모드는 프로젝트(여행) 단위로 공존 — 한 사람이 어떤 여행은 로컬로만, 어떤 여행은 클라우드 동기화로 쓸 수 있음

## 6. 리스크 및 대응

| 리스크 | 대응 |
|---|---|
| Phase 1: 브라우저 데이터 삭제·기기 변경 시 데이터 유실 | 내보내기(백업) 기능을 Phase 1과 함께 필수로 출시하고, UI에서 주기적으로 백업을 권장 |
| Phase 2: Supabase 무료 티어 한도(DB 500MB, 월 5GB 전송) 소진 | 지인 규모(수십 명)에서는 여유 있음. 사진 업로드가 주요 소모 요인이므로 업로드 시 용량 제한/압축 권장 |
| Phase 2: Cloudinary 무료 티어(월 25 크레딧) 소진 | 사진 갤러리 사용량 모니터링, 원본 대신 압축본 업로드 유도 |
| Google OAuth 설정 필요 | Google Cloud Console에 OAuth 클라이언트 등록 필요(1회성 작업). 사용자 수가 늘면 OAuth 동의 화면 검수가 필요할 수 있음 — Open Question 참고 |
| 악의적 가입/스팸 | Phase 1은 백엔드가 없어 원천적으로 해당 없음. Phase 2는 실제 구글 계정이 있어야 하고 RLS로 서로 데이터 접근이 막혀 있어 스팸으로 인한 피해가 제한적 — 별도 CAPTCHA 등 추가 방어는 현재 규모에서 과함(YAGNI) |
| 공유 링크 무한 재사용 | 소유자가 링크를 끄고 재발급할 수 있는 기능으로 통제권 유지 |

## 7. Open Questions

- IndexedDB의 브라우저별 저장 용량 한도와, 한도에 가까워졌을 때 사용자에게 보여줄 안내 문구/UX가 아직 미정
- Google OAuth 동의 화면을 "테스트 사용자"로 제한(100명 이하 지인 규모면 충분)할지, "프로덕션 게시"로 전환할지 — 초대 인원 규모가 커지면 재검토 필요
- Phase 2 전환 시, 기존 PIN 잠금으로 쓰던 가족 구성원에게 로그인 전환을 어떻게 안내할지 (PIN이 아예 사라지고 구글 로그인만 남기 때문에 사전 공지가 필요)
